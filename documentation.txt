1)var - создание переменной
	var a = 5 * (1 + 4)
	var a = var c = var d = 5
	var b = a
	5 + (var a = 5) - скобки важны
2)null
	Вернёт 0

3) if expr then expr
   elif expr then expr
   else expr


Правки:
	аннотация типов
	распределить по файлам
	почитать про встроенные методы класса
	перечитать код и попробовать изменить  некоторые элементы